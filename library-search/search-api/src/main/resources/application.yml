spring:
  profiles:
    active: local
  config:
    import: application-naver-client.yml
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:lsdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true